Print the current workspace state.

Steps:
1. Read the top-level `CONTEXT.md` and display its full contents.
2. If the user has mentioned a specific client, also read `clients/{name}/CONTEXT.md` and display it.
3. Summarize in 2-3 sentences: what's active, what's blocked, what's next.
